
1. 区分数据库，本地模式，使用本地的数据库，配置信息如下，上线后发布用的是之前配置的在线数据库
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=daziwordsapp
DB_USER=root
DB_PASSWORD=<local-password>
DB_POOL_SIZE=10
DB_CONNECT_TIMEOUT_MS=5000

2. 所有的数据，在本地调试，测试，发布的时候，保留一个脚本命令，将本地所有的数据库里的数据，全量覆盖线上数据库。

3. 补充一个登录模块，密码用明文存储在数据库里，不需要什么验证，测试项目，没必要，需要了再改进。项目登录了才能访问，所以你需要登录验证校验等逻辑，前端页面对于登录校验的拦截等等，你来处理。

4. 现在，保留数据库里的所有数据，技术改造整个项目，新项目放在 D:\program\japanese-vocabulary-site-v2 里，在这个目录里，前端放在 frontend 目录下，后端放在backend目录下。

前后端分离，使用nest.js作为所有的后端服务，配套使用各种辅助工具。
前端使用 react，ui 库使用 @chakra-ui/react，确保现在的整体交互风格不变。

前端的脚手架，使用 vite 构建 ts 版本的 react，需要的工具你就自己添加。
后端使用 nest-cli 生成，也是 ts 版本。

5. 后端的模块划分，有要求，参考 D:\program\dazi_words_app\dazi_words_backend 这个目录下的src的目录结构，分拆 common config entities filters helpers interceptors middlewares modules pipes shared-modules types 等等

注意，entities 里的每一个 entity ，都是由自己的配置，对外统一暴露是通过 index.ts，然后给到 share-modules 里做共享。

modules 里，每一个 module 有 service controller dto config，但是，只参考模块结构，不要直接用代码，参考即可。

6. 前端的模块划分，也要有要求，参考 D:\program\dazi_words_app\dazi_words_frontend 目录下的 src 目录里的内容，路由、组件、页面、布局、该分拆就要分拆。

7. 注意哈，你生成的代码，都是在D:\program\japanese-vocabulary-site-v2 目录下的 frontend 和 backend ，把一些文档，文件，之类的固定的内容，放在 docs 目录下，docs, frontend, backend 三个目录平级，前后端都需要自己的 readme，放在自己的目录下，最外部也需要一个 readme。

8. 开发的时候，前后端两个服务，通过代理服务调用。构建发布的时候，将前端构建后的产物，全部拷贝到backend 的 web目录下，作为前端的静态资源。后端使用中间件，将这些资源作为页面访问请求处理。所有的业务API接口，都用 api 作为根路径，与页面路由区分。

9. 最后部署的时候，可能需要 chatGPT 或 cloudflare 的一些功能服务，可以直接编写对应的代码，脚本处理。

10. nest 里的数据库服务哈，尽量使用链式的js代码生成SQL语句，减少直接使用SQL语句。

11. 还需要 swagger 的接口文档。

12. 这次项目改造。把原来的 D:\program\japanese-vocabulary-site 的 git 拷贝过来，沿用旧有的 git 记录，我不放在一块，是因为我可能还要用到一些代码。但是你要知道，我只是把代码放在另一个目录里，不是新启一个项目。

13. 本地数据库如果连不上，先不考虑数据库的问题，主要是改造代码。

开始执行，授予你访问 D:\program\dazi_words_app 目录下的所有文件的查看权限。授予你 D:\program\japanese-vocabulary-site-v2 目录下的读写权限。



==================================================================
==================================================================
==================================================================
==================================================================


后端的修改项

1. 项目里缺少日志，对于HTTP操作，DB操作，业务逻辑，异常捕获等，必须全部输出日志到文件模块。日志文件放在项目目录的根目录的 logs 目录下，不提交到git。文件命名，jvs-日期-001 类似于这样的日志。

但是日志内容你要区分模块的，有一些是HTTP，前边就要有HTTP，DB 操作，业务判断，异常等成。

服务启动后，在日志里输出服务的 本地 URL 和 接口文档的 URL。

可以采用 nest 的日志模块，具体你可以处理。最终执行的 SQL 语句，也要写到日志里，不过不要阻塞主进程业务执行。

2. 前后端项目，都缺少注释说明，代码之间的空行处理。不要都放在一起，该空行要空行。

3. 你的模块划分有问题啊，知识资源怎么都在一块了，词汇、语法、句子、集合、标签、等等，各自要在自己的模块里，可以作为知识资源的子模块，但是不要都放在一块。

4. 这些常量，不要直接放在业务代码里，要放在模块的配置文件里

const DETAIL_PATHS = RESOURCE_PATHS.map((value) => `${value}/:id`);

5. import 和 export 与 class function 这些模块之间，要有空行分割，大概是这样。

import XXX
import YYY

export ZZZ

const LLL = 

class AAA {


	@xxx
	m;

	@uuu
	n;

	x;

	y;

	a() {

	}

	n(){

	}
}

function m() {

}

6. mysql 的 entity 里，要有每一个表的说明，每一个字段的说明，就用 Entity Column 自带的，可以生成到 sql 表里的


接下里是前端的修改项

1. 前端页面，导航要用横向的，在页面顶部。左侧是 logo 标题，挨着是导航，最右侧是个人的名字和退出按钮。

2. 导航有

首页 集合 词库 语法 句子 复习 管理 这几个模块。

各个页面的交互样式，参考 docs/jsv-printscreen 目录里的各个图片。

其实就是参考现在 https://japanese-vocabulary-site.tery-bd4.workers.dev/ 这个项目里的页面，交互，样式，实现现有的需求。


==================================================================
==================================================================
==================================================================
==================================================================


1. 导航，使用 ui 库里的 Breadcrumb 组件。

2. 所有的页面，每一个页面里的内容，都必须是一个单独的页面组件，目录结构是这样的

pages、layout、conpoments/ 		这3个目录
|---login/
|-------index.tsx 	对外暴露的文件
|-------Login.tsx 	主要的组件
|-------types.tsx 	类型，接口等
|-------config.tsx 	配置文件
|-------utils.tsx 	辅助需要的各种方法什么的
|-------components/ 这个组件需要用到的自己的组件
|-----------LoginHeader.tsx 	用到的组件
|-----------LoginFooter.tsx 	用到的组件

要用这样的结构，注意，types config utils comonents 不一定有。


3. 每一个页面，都必须有一个自己单独的页面级组件，不允许多个页面共用一个页面组件。

4. 所有的表单，是使用 inline 模式，左侧是标签，右侧是表单空间，左对齐。

5. 词汇集合页面的每一个集合的卡片，右上角的三个点，点击后出来两个按钮，类似于 BottonDrops 这样的样式，出来两个按钮，一个是编辑，一个是删除。

编辑的话，就打开弹窗，对集合内容编辑。
删除的话，就打开弹窗，确认删除。是逻辑删除。

6. 集合里的词汇测试，每次测试10个词汇，测试完毕，就显示出这10个词汇清单，把错误的加入到错题本里。
然后下边有两个按钮，一个是再来一组，一个是返回。那就返回到上一个页面。

7. 集合页面学习词汇、词库页面学习词汇，有通用的逻辑，就是页面有 词汇、假名、翻译、默记模式四个复选框，注意【词汇、假名、翻译】这三个是一组的，与【默记模式】是互斥的。如果默记模式勾选了，那么【词汇、假名、翻译】这三个就不能操作了。

词汇、假名、翻译，这三个复选框，就是一个开关，点击后，控制表格里的词汇、假名、翻译 显示或隐藏，隐藏的时候，显示三个点。

默记模式勾选后，表格里只有词汇正常显示，假名、翻译全部都隐藏掉。但是点击词汇、假名、翻译，依然是可以切换显示隐藏的效果。

这个词汇表格里的词汇，点击后，进入词汇详情页面

8. 词汇详情页面，可以造句，造句实际上是调用句子页面里的创建句子的功能。
因为词汇、语法、句子，是可以互相关联的。初始是不关联的。

所以，你要把新增词汇、句子、语法的这个功能，全部抽象成一个组件，在各个地方调用。

新增词汇的时候，可以选择关联的语法，可以选择关联的句子。

语法、句子也是这样。


在语法详情页面也需要有关联词汇、句子的功能。
在句子详情页面也需要有关联词汇、语法的功能。

当然，每一个详情页面，都有自己的编辑功能。


关联的功能，要做成弹窗浮层来操作。

关联关闭，在页面上，就可以直接看到这个详情关联到的其他的内容。


这三个详情页面，必须是互相孤立的，不得使用同一个页面文件。


9. 新增、编辑 词汇、语法、句子时，可能有表单需要选择 词性、标签、所属集合 这种多项的信息。

还是要用表单，但是不要直接使用多个 checkbox 平铺，要用一个可以下拉输入筛选，但是每一个选项又不是自己独自占一行的表单，这个可能没有现成的，得你自己写一个这样的组件，因为要通用这种组件。

10. 所有的增删改查，都要有一个页面级别的 loading 效果。特别是增删改，避免重复操作。

11. 所有的时间，都使用 YYYY-MM-DD HH:mm:ss 的格式

12. 去掉管理页面的【导入审核】，词性页面，列表，把【名词、自动词、他动词、サ变动词、な形容词、い形容词、副词】的排序放在最高，第一个是10，第二个是20，依次修改排序号，查询的时候，按照从小到大的顺序排，新增的时候，要按照最大的序号再加10。

名称放第一列，代码放第二列，序号第三列，状态第四列，序号不可编辑。

13. 标签页面，颜色，要固定设置，在代码里，固定列出10个颜色，下拉选择的时候，直接选择对应的颜色块，注意要渲染这个颜色快，这样便于操作。颜色只能从固定的里边选，代码里写10个16进制的颜色，可以从组件库里调，UI 要好看，不影响文字。标签要作为词汇、语法、句子里的标签渲染颜色背景。所以不能跟文字颜色冲突。

14. 整体的网站颜色风格，过于蓝色，你能在代码里搞页面主题颜色变量配置吗？ 我的意思是，现在是蓝色，可以切换为橙色，绿色什么的，定义变量，减少固定硬编码。

该成一个橙黄色的主题样式。 chakra 应该有这种功能配置
css 使用 less 吧。

15. 对了，整个页面的UI交互，需要支持移动端操作，所以移动端，就不要使用表格了，表格不行的。所有表格，都转为卡片，然后去掉表格的按钮操作。显示信息即可。

16.

  logger.log(`server is running at ${localUrl}`);
  logger.log(`server docs is running at ${swaggerUrl}`);

  这两行为什么会输出日志


  [Nest] 17012  - 2026/08/11 18:58:12     LOG [JVS] server is running at http://localhost:3000
[Nest] 17012  - 2026/08/11 18:58:12     LOG [JVS] undefined
[Nest] 17012  - 2026/08/11 18:58:12     LOG [JVS] server docs is running at http://localhost:3000/api/docs
[Nest] 17012  - 2026/08/11 18:58:12     LOG [JVS] undefined

有两个 undefined 是什么

17. api 接口，不要使用 restful 风格的接口，这种风格绝对不能用。

就直接一些，简单一些，所有查询的，都用 get，所有增删改用 post

接口的 url，直接就是 
/api/vocabularies?wordId=xxx 查询词汇详情
/api/vocabularies/relations?wordId=xxx&targetResource 
类似于这样的， 不要搞什么 restful ，URL 又臭又长


18. 前端这里

    await request({
      method: "PUT",
      url: "/settings",
      data: { key: "pagination_defaults", value: values },
    });

    把所有的接口，都封装到单个的方法，不允许这样随意定义使用，使用的时候要调用方法，避免重复定义接口。接口的方法，放在 http/api目录下

19. 你把登录的验证给加上了，给你说了，这是测试用的， 不用加，全部都不用加密，直接明文存储密码，你他妈不要违背我的命令呀。

开始执行，逐条检查执行的效果，不允许偷懒。我要逐条检查。




==================================================================
==================================================================
==================================================================
==================================================================

1. 改正错误 
http://localhost:5173/api/collections/delete
{"collectionId":4}
{
    "success": false,
    "data": null,
    "message": "Property \"deleted_at\" was not found in \"CollectionEntity\". Make sure your query is correct."
}

2. 控制台警告信息

[Nest] 24632  - 2026/08/11 22:09:02    WARN [LegacyRouteConverter] Unsupported route path: "/api/*". In previous versions, the symbols ?, *, and + were used to denote optional or repeating path parameters. The latest version of "path-to-regexp" now requires the use of named parameters. For example, instead of using a route like /users/* to capture all routes starting with "/users", you should use /users/*path. For more details, refer to the migration guide. Attempting to auto-convert to "/api/{*path}"...





