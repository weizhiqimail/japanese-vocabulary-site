import { KnowledgeResourceList } from "@/components/resources/KnowledgeResourceList";

/** 词库独立页面级组件。 */
export function Vocabularies() {
  return <KnowledgeResourceList resource="vocabularies" />;
}
