import type { BaseEntityConfig } from '@/entities/base-entity-config.interface';
export const CollectionVocabularyEntityConfig: BaseEntityConfig = {
  dbName: 'collection_vocabularies',
  comment: '集合词汇成员',
};
