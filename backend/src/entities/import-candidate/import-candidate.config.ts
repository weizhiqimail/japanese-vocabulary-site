import type { BaseEntityConfig } from '@/entities/base-entity-config.interface';
export const ImportCandidateEntityConfig: BaseEntityConfig = {
  dbName: 'import_candidates',
  comment: '非正式词汇审核池',
};
